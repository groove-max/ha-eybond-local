{
  "extends": "../base.json",
  "profile_key": "modbus_smg/models/aninerel_anl_4200t_24l_w_pro.json",
  "title": "Aninerel ANL-4200T-24L-W-PRO (untested SMG controls, unvalidated voltage windows)",
  "driver_key": "modbus_smg",
  "protocol_family": "modbus_smg",
  "capabilities": [
    {
      "key": "battery_bulk_voltage",
      "minimum": 0,
      "maximum": 700,
      "note": "24 V unit: the exact setpoint limits for this model are undocumented, so the window deliberately spans both battery classes and the inverter's own write validation is the authority; a rejected write surfaces as a capability block."
    },
    {
      "key": "battery_float_voltage",
      "minimum": 0,
      "maximum": 700,
      "note": "24 V unit: the exact setpoint limits for this model are undocumented, so the window deliberately spans both battery classes and the inverter's own write validation is the authority; a rejected write surfaces as a capability block."
    },
    {
      "key": "battery_under_voltage",
      "minimum": 0,
      "maximum": 700,
      "note": "24 V unit: the exact setpoint limits for this model are undocumented, so the window deliberately spans both battery classes and the inverter's own write validation is the authority; a rejected write surfaces as a capability block."
    },
    {
      "key": "battery_under_voltage_off_grid",
      "minimum": 0,
      "maximum": 700,
      "note": "24 V unit: the exact setpoint limits for this model are undocumented, so the window deliberately spans both battery classes and the inverter's own write validation is the authority; a rejected write surfaces as a capability block."
    },
    {
      "key": "battery_redischarge_voltage",
      "minimum": 0,
      "maximum": 700,
      "note": "24 V unit: the exact setpoint limits for this model are undocumented, so the window deliberately spans both battery classes and the inverter's own write validation is the authority; a rejected write surfaces as a capability block."
    },
    {
      "key": "battery_overvoltage_protection_voltage",
      "minimum": 0,
      "maximum": 700,
      "note": "24 V unit: the exact setpoint limits for this model are undocumented, so the window deliberately spans both battery classes and the inverter's own write validation is the authority; a rejected write surfaces as a capability block."
    },
    {
      "key": "battery_equalization_voltage",
      "minimum": 0,
      "maximum": 700,
      "note": "24 V unit: the exact setpoint limits for this model are undocumented, so the window deliberately spans both battery classes and the inverter's own write validation is the authority; a rejected write surfaces as a capability block."
    }
  ]
}
